# Application Unit Tests

Tests for the **Application** layer, focused on validating components in isolation.

## Responsibilities
- Validate component behavior in isolation
- Verify validation rules
- Test pipeline behaviors

## Dependencies
- Depends on **Application** and **Infrastructure**
- Must not depend on **Graph**
- Uses external testing libraries

## Notes
These tests focus on individual components rather than complete use cases.
