# Graph.DataLoaders Layer

The **Graph.DataLoaders** project provides data loading components used during GraphQL execution.

## Responsibilities
- DataLoader implementations
- Request-scoped batching and caching
- Coordinated data fetching

## Dependencies
- Depends on **Application** and **Infrastructure**

## Notes
This layer optimizes data fetching during GraphQL execution through batching and request-scoped caching.